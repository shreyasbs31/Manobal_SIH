import { EmptyState } from "@manobal/ui";

export default function RosterPage() {
  return (
    <div className="mb-home-stack">
      <h2>Project 14 days</h2>
      <p>
        Company sliders stay at company level. Groups under 10 cannot be
        simulated alone.
      </p>
      <EmptyState
        message="Coverage and posture will sit side by side after the balancer is wired."
        title="Projection ready"
      />
    </div>
  );
}

import { EmptyState } from "@manobal/ui";

export default function DpoPage() {
  return (
    <EmptyState
      message="Access, correction, erasure, nomination, and grievance requests will list here with due dates."
      title="No open rights requests"
    />
  );
}

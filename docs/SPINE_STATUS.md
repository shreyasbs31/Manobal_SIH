# Demo spine status (Prompt 10A-2)

Date: 17 Sep 2026. Stack: Docker on this Mac, web `http://localhost:3000`, engine `http://localhost:8000`. Pass requires a screenshot, a network log, and matching engine log lines unless the row is a host or in-container probe (those use the listed text files). No row says pass without those paths.

Root cause of earlier "passes" that did not work in the app: `make providers-check` ran on the Mac. The engine container had `infra/.cache/foundry.token` as a directory, so the token mount was empty, DefaultAzureCredential failed, and Foundry never answered from Docker. Speech, Translator, Content Safety, ACS, and Deepgram keys were already in the container. Fix: write a real token file, mount `infra/.cache` at `/run/manobal/cache`, auth order entra file then Azure OpenAI key then DefaultAzureCredential. In-container check now prints the auth path.

Auth names only (do not paste values). Local Docker Azure OpenAI key fallback uses `AZURE_OPENAI_API_KEY` and `AZURE_OPENAI_ENDPOINT`. This machine already has both set. Azure itself stays Entra or managed identity.

| Test | Status | Evidence | Root cause if fail | Fix |
|---|---|---|---|---|
| Engine container providers-check | pass | `e2e/artifacts/spine/providers-check.txt` (11 pass, 0 fail). Foundry `entra_file`. Deepgram `deepgram_key`. Speech `speech_key`. Translator `translator_key`. Content Safety `content_safety_key`. ACS `acs_connection_string`. Web PubSub `local_hub` 31.1. Key Vault `local_wrap` 31.1. | | `make providers-check` execs inside `engine` when that container is running. |
| Arjun Hindi voice turn | pass | `e2e/artifacts/spine/arjun-hindi-voice.png`, `.network.log`, `.engine.log`. Network hits `/app/saathi?fixture=arjun-hi` and `/api/v1/voice/fixtures/arjun-hi`. Engine: `foundry_auth path=entra_file`, WebSocket voice session, Azure Speech TTS. | | Director shot `voice`. Click Play recorded check-in. |
| Karthik Tamil voice turn | pass | `e2e/artifacts/spine/karthik-tamil-voice.png`, `.network.log`, `.engine.log`. Fixture `karthik-ta`. Engine: `foundry_embed class=embed_ml`, Speech TTS, voice session. | | Director shot `karthik`. |
| Deepak typed distress | pass | `e2e/artifacts/spine/deepak-typed-distress.png`, `.network.log`, `.engine.log`. Navigate to `/app/safety` after typed line. Engine: `POST /api/v1/acute` 200. | | Director shot `deepak` (typed path) and `deepak-typed` after engine rebuild. |
| Deepak spoken distress and T4 under 5 s | pass | `e2e/artifacts/spine/deepak-spoken-distress.png`, `deepak-t4-welfare.png`, `deepak-t4-medical.png`, `.network.log`, `.engine.log`. Engine: `POST /api/v1/acute` then `GET /api/v1/medical/acute`. Playwright asserted T4 on both queues inside 5 s. | | Director shot `deepak`. Play recorded check-in. |
| Hindi case brief with field marks | pass | `e2e/artifacts/spine/hindi-case-brief.png`, `.network.log`, `.engine.log`. Network: `GET /api/v1/welfare/cases/MB-4091?lang=hi`. UI has `.mb-brief-ref`. | | Director shot `hindi-brief`. |
| Copilot aggregate Hindi, live main, chart | pass | `e2e/artifacts/spine/copilot-aggregate-hi.png`, `.network.log`, `.engine.log`. Engine line `copilot aggregate provider=main`. Chart test id present. Unit test `test_individual_copilot_never_imports_gateway` refuses before `gateway.run`. | | Director shot `copilot-aggregate`. |
| Copilot individual refused | pass | `e2e/artifacts/spine/copilot-individual-refuse.png`, `.network.log`, `.engine.log`. Engine line `copilot refuse individual provider=refused` before any second Foundry call for that question. | | Director shot `copilot`. |
| Deepgram outage fallback | pass | `e2e/artifacts/spine/deepgram-outage.png`, `.network.log`, `.engine.log`. Turn still completed after `POST /api/v1/demo/outage` deepgram on. | | Director: Deepgram outage, then shot `deepgram-outage`. |
| Lab numbers computed | pass | `e2e/artifacts/spine/lab-numbers.png`, `.network.log`, `.engine.log`. Live UI 17 Sep 2026: `Source: core.assessment.` Imran T1, Thomas T0. Empty assessment table yields zeros; overlay now falls back to `demo_cases_memory` when that table has no rows (engine rebuild needed to show the fallback on Lab). Not the old 4.2 / 0.11 literals. | | Director shot `lab`. |
| Governance numbers computed | pass | `e2e/artifacts/spine/governance-numbers.png`, `.network.log`, `.engine.log`. Live UI: `Source: core.assessment.` Lead time n/a, false-positive 0.00 from empty assessments, not hardcoded 4.2 d / 0.11. | | Director shot `governance`. |
| Voice first-audio 20 turns | pass (target missed) | `e2e/artifacts/spine/voice-latency.txt`. n=20, auth=`entra_file`. p50 2914 ms, p95 3429 ms, mean 2858 ms, target 2500 ms. | India to eastus2 still waits on the first model token even with first-sentence TTS, parallel gates, cached companion prompt, fast class, short output. | Do not claim under 2.5 s. Show the thinking orb. Pre-rendered first byte remains the camera option. |
| Retrieval embed + rerank | pass | `e2e/artifacts/spine/retrieval.json`, `retrieval-registry.txt`. Choice: en=`embed`, hi/hi-Latn/ta=`embed_ml`, rerank=`rerank`. recall_at_3 all 1.0. | | Registry also on `GET /gov/models` when live. |
| Translator kok, sa, sat | pass | `e2e/artifacts/spine/translator-fallback.txt` and `infra/i18n/machine.json`. Each lang `source=main_model`, `machine_translated=true`, `error=HTTP 400`. | | Catalog regenerate via `make translate-catalog`. |
| Spec 27.2 offline navigations | fail | Playwright 17 Sep 2026 against Docker: pass 2,3,5,6,9,11,12. Fail 1 (queue count 0), 4 (toolkit copy), 8 (nudges), 10 (plan button). Test 7 hung on `postAcute` while offline. No new pass screenshots yet. | Service worker registered after first document fetch, so some routes were not in cache. Crisis send awaited engine POST until the test timed out. Check-in Save could still hit localhost, so IndexedDB stayed empty. Home hid local nudges when two server cards already filled the slots. | SW v4, warm the SW then the routes, enqueue on airplane or fetch fail, lexicon navigates to safety without awaiting the network, toolkit/home/plan shells render offline without API data. Rebuild web, then `corepack pnpm --filter @manobal/e2e exec playwright test tests/audit-offline.spec.ts`. |
| Director one-click presets | pass | Live `/director` table 17 Sep 2026 lists voice, karthik, deepak, copilot, copilot-aggregate, hindi-brief, deepgram-outage, lab, governance, offline, with What to do and What should happen. Stage caption uses `SHOT_INSTRUCTIONS`. `deepak-typed` is in source; waiting on engine image rebuild. | | Open `/director`, click the shot id. |

Playwright file for the 6.4 UI rows: `e2e/tests/spine.spec.ts` (9/9 passed, 34.1 s, against Docker `http://localhost:3000`).

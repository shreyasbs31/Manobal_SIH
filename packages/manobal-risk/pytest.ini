[pytest]
# Isolated from the repo-root Django config. CI installs only this package,
# so --reuse-db and DJANGO_SETTINGS_MODULE must not appear here.
addopts = -q --import-mode=importlib --strict-markers
testpaths = tests
python_files = test_*.py
